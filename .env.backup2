DATABASE_URL=file:/home/z/my-project/data/submissions.db

# CSRF Protection (generate a random 32-character string)
# In production, generate a secure random string using: openssl rand -hex 32
CSRF_SECRET=change-this-in-production-use-a-32-char-random-string-here

# Supabase Configuration
# Get these from your Supabase project dashboard: https://supabase.com/dashboard
# Leave as placeholders if not using Supabase
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url-here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Email Configuration (optional - add your email service credentials)
# Example using Resend or SendGrid
# Uncomment and configure these when ready to use email notifications
# SMTP_HOST=smtp.resend.com
# SMTP_PORT=587
# SMTP_USER=resend
# SMTP_PASSWORD=your-resend-api-key
# SMTP_FROM=noreply@surbhutech.com

# Security Configuration
# NODE_ENV should be set to 'production' in production
NODE_ENV=development

# Rate Limiting Configuration
# These values are configured in src/lib/rate-limit.ts
# CONTACT_FORM_LIMIT_WINDOW=60000 (1 minute in ms)
# CONTACT_FORM_LIMIT_MAX=3 (max 3 submissions per minute)

# Allowed CORS Origins (comma-separated)
# Configure this in production with your actual domain
ALLOWED_ORIGINS=https://surbhutech.com,http://localhost:3000
